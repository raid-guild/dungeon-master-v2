table:
  schema: public
  name: raid_statuses
array_relationships:
  - name: raids
    using:
      foreign_key_constraint_on:
        column: status
        table:
          schema: public
          name: raids
