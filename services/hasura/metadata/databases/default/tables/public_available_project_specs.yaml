table:
  name: available_project_specs
  schema: public
is_enum: true
array_relationships:
  - name: consultations
    using:
      foreign_key_constraint_on:
        column: specs_key
        table:
          name: consultations
          schema: public
select_permissions:
  - role: member
    permission:
      columns:
        - available_project_spec
      filter: {}
