table:
  name: budget_options
  schema: public
is_enum: true
array_relationships:
  - name: consultations
    using:
      foreign_key_constraint_on:
        column: budget_key
        table:
          name: consultations
          schema: public
select_permissions:
  - role: member
    permission:
      columns:
        - budget_option
      filter: {}
