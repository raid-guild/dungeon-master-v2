table:
  name: clients
  schema: public
array_relationships:
  - name: clients_contacts
    using:
      foreign_key_constraint_on:
        column: client_id
        table:
          name: clients_contacts
          schema: public
  - name: consultations_clients
    using:
      foreign_key_constraint_on:
        column: client_id
        table:
          name: consultations_clients
          schema: public
select_permissions:
  - role: member
    permission:
      columns:
        - name
        - website
        - id
      filter: {}
