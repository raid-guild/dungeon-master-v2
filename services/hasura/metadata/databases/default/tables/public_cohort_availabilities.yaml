table:
  name: cohort_availabilities
  schema: public
is_enum: true
array_relationships:
  - name: applications
    using:
      foreign_key_constraint_on:
        column: availability
        table:
          name: applications
          schema: public
