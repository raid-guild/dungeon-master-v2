table:
  schema: public
  name: cohort_availabilities
is_enum: true
array_relationships:
  - name: applications
    using:
      foreign_key_constraint_on:
        column: availability_key
        table:
          schema: public
          name: applications
