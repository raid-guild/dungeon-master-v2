table:
  name: cohort_availabilities
  schema: public
is_enum: true
array_relationships:
  - name: applications
    using:
      foreign_key_constraint_on:
        column: availability_key
        table:
          name: applications
          schema: public
select_permissions:
  - role: member
    permission:
      columns:
        - cohort_availability
      filter: {}
