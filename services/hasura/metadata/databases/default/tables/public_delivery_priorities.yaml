table:
  name: delivery_priorities
  schema: public
is_enum: true
array_relationships:
  - name: consultations
    using:
      foreign_key_constraint_on:
        column: delivery_priorities_key
        table:
          name: consultations
          schema: public
