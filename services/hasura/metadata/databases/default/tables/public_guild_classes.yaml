table:
  schema: public
  name: guild_classes
is_enum: true
array_relationships:
  - name: members
    using:
      foreign_key_constraint_on:
        column: guild_class
        table:
          schema: public
          name: members
  - name: portfolios_roles
    using:
      foreign_key_constraint_on:
        column: role
        table:
          schema: public
          name: portfolios_roles
  - name: raids_roles_requireds
    using:
      foreign_key_constraint_on:
        column: role
        table:
          schema: public
          name: raids_roles_required
