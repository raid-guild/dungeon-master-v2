table:
  name: guild_classes
  schema: public
is_enum: true
array_relationships:
  - name: members
    using:
      foreign_key_constraint_on:
        column: guild_class
        table:
          name: members
          schema: public
  - name: portfolios_roles
    using:
      foreign_key_constraint_on:
        column: role
        table:
          name: portfolios_roles
          schema: public
  - name: raids_roles_requireds
    using:
      foreign_key_constraint_on:
        column: role
        table:
          name: raids_roles_required
          schema: public
