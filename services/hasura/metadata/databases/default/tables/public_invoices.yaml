table:
  name: invoices
  schema: public
array_relationships:
  - name: raids
    using:
      foreign_key_constraint_on:
        column: invoice_id
        table:
          name: raids
          schema: public
