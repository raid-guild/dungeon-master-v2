table:
  schema: public
  name: member_types
is_enum: true
array_relationships:
  - name: members
    using:
      foreign_key_constraint_on:
        column: member_type
        table:
          schema: public
          name: members
