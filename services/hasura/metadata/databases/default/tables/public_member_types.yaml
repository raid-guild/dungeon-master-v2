table:
  name: member_types
  schema: public
is_enum: true
array_relationships:
  - name: members
    using:
      foreign_key_constraint_on:
        column: member_type_key
        table:
          name: members
          schema: public
