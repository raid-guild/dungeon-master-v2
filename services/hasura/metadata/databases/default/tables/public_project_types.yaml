table:
  name: project_types
  schema: public
is_enum: true
array_relationships:
  - name: consultations
    using:
      foreign_key_constraint_on:
        column: type_key
        table:
          name: consultations
          schema: public
select_permissions:
  - role: member
    permission:
      columns:
        - project_type
      filter: {}
