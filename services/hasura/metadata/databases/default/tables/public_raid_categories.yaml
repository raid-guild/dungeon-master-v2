table:
  schema: public
  name: raid_categories
is_enum: true
array_relationships:
  - name: portfolios
    using:
      foreign_key_constraint_on:
        column: category
        table:
          schema: public
          name: portfolios
  - name: raids
    using:
      foreign_key_constraint_on:
        column: category_key
        table:
          schema: public
          name: raids
