table:
  schema: public
  name: raid_parties
object_relationships:
  - name: memberByMember
    using:
      foreign_key_constraint_on: member
  - name: raidByRaid
    using:
      foreign_key_constraint_on: raid
select_permissions:
  - role: member
    permission:
      columns:
        - created_at
        - updated_at
        - v1_id
        - id
        - member
        - raid
      filter: {}
