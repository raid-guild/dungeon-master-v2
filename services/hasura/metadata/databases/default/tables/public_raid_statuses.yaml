table:
  name: raid_statuses
  schema: public
is_enum: true
array_relationships:
  - name: raids
    using:
      foreign_key_constraint_on:
        column: status_key
        table:
          name: raids
          schema: public
