table:
  schema: public
  name: raid_statuses
is_enum: true
array_relationships:
  - name: raids
    using:
      foreign_key_constraint_on:
        column: status_key
        table:
          schema: public
          name: raids
