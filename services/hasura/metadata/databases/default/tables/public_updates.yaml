table:
  name: updates
  schema: public
object_relationships:
  - name: member
    using:
      foreign_key_constraint_on: member_id
  - name: raid
    using:
      foreign_key_constraint_on: raid_id
select_permissions:
  - role: member
    permission:
      columns:
        - update
        - created_at
        - updated_at
        - id
        - member_id
        - raid_id
      filter: {}
